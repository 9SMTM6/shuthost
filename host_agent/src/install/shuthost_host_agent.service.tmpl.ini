[Unit]
Description={ description }

[Service]
Environment=SHUTHOST_SHARED_SECRET={ secret }
ExecStart={ name } service --port={ port } --broadcast-port={ broadcast_port } --shutdown-command="{ shutdown_command }" --hostname="{ hostname }"
Restart=always
User=root
Group=root

[Install]
WantedBy=multi-user.target
