[workspace]
resolver = "2"
members = ["agent", "controller"]

[workspace.dependencies]
hmac = "0.12.1"
serde = { version = "1.0.219", features = ["derive"] }
sha2 = "0.10.8"
toml = "0.8.20"
hex = "0.4.3"

[workspace.package]
version = "0.0.5"
edition = "2024"
authors = ["Samuel Maier"]
description = "Adds the ability to remotely control the standby of linux (systemd) based hosts"
documentation = "https://example.com/bar"
