{
  "name": "frontend-tests",
  "version": "1.0.0",
  "description": "Playwright-based frontend testing for shuthost WebUI.",
  "scripts": {
    "test": "playwright test",
    "install-chromium": "npx playwright install chromium"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@playwright/test": "^1.43.0",
    "@types/node": "24.X"
  }
}
